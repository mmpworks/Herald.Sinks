<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Herald.Sinks.Pulsar</RootNamespace>
    <AssemblyName>Herald.Sinks.Pulsar</AssemblyName>
    <PackageId>MMP.Herald.Sinks.Pulsar</PackageId>
    <Version>1.0.0</Version>
    <Description>Sends Herald log events to an Apache Pulsar topic via DotPulsar. Multi-tenant pub/sub for streaming + queue patterns.</Description>
    <PackageTags>herald;logging;sink;pulsar;streaming</PackageTags>
    <IsAotCompatible>false</IsAotCompatible>
    <DefaultItemExcludes>$(DefaultItemExcludes);tests\**;docs\**;build\**</DefaultItemExcludes>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="DotPulsar" />
  </ItemGroup>
  <ItemGroup>
    <None Include="CAPABILITY.yaml" Pack="true" PackagePath="\" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="Herald.Sinks.Pulsar.Tests" />
  </ItemGroup>

  <!-- Batching decorator: the provider wraps its sink with
       BatchingLogSinkDecorator.Wrap so the producer never blocks on
       delivery. Shared utility package, referenced like Herald.OSS. -->
  <ItemGroup>
    <ProjectReference Include="../Herald.Sinks.Batching/Herald.Sinks.Batching.csproj" />
  </ItemGroup>
</Project>
