<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Herald.Sinks.NewRelicLogs</RootNamespace>
    <AssemblyName>Herald.Sinks.NewRelicLogs</AssemblyName>
    <PackageId>MMP.Herald.Sinks.NewRelicLogs</PackageId>
    <Version>1.0.0</Version>
    <Description>Ships Herald log events to the New Relic Logs ingest API. Drop-in for Serilog.Sinks.NewRelic.Logs.</Description>
    <PackageTags>herald;logging;sink;newrelic;observability</PackageTags>
    <IsAotCompatible>true</IsAotCompatible>
    <DefaultItemExcludes>$(DefaultItemExcludes);tests\**;docs\**;build\**</DefaultItemExcludes>
  </PropertyGroup>
  <ItemGroup>
    <None Include="CAPABILITY.yaml" Pack="true" PackagePath="\" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="Herald.Sinks.NewRelicLogs.Tests" />
  </ItemGroup>

  <!-- Batching decorator: the provider wraps its sink with
       BatchingLogSinkDecorator.Wrap so the producer never blocks on
       delivery. Shared utility package, referenced like Herald.OSS. -->
  <ItemGroup>
    <ProjectReference Include="../Herald.Sinks.Batching/Herald.Sinks.Batching.csproj" />
  </ItemGroup>
</Project>
